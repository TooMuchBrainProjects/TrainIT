@typeparam TData 

@if (Data is not null)
{
    @ChildContent(Data)
}
else
{
    <MudProgressCircular Color="Color.Primary" Indeterminate="true"/>
}

@code {

    [Parameter]
    public List<TData>? Data { get; set; }

    [Parameter]
    public RenderFragment<List<TData>> ChildContent { get; set; } = null!;
}